platform = espressif32
framework = arduino
board_build.partitions = huge_app.csv

# The bluetooth library is very large so the default partition table with 1.4 MB for the application may be to small.
# No OTA support and less space for SPIFFS but up to 3 MB for the application with huge_app.csv.
# No OTA support but more space for SPIFFS and up to 2 MB for the application with no_ota.csv.
